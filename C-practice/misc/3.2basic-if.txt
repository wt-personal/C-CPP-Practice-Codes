#include <stdio.h>

int main(){
    int num1,num2;
    printf("Give number 1: ");
    scanf("%d", &num1);
    printf("Give number 2: ");
    scanf("%d", &num2);
    
    printf("User did input numbers %d and %d and the bigger one is ",num1,num2);
    
    if (num1 > num2) printf("%d",num1);
    else printf ("%d",num2);
}